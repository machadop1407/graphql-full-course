/* Write a fragment for the type MOVIE described below. The fragment should include the fields: title, director
 *
 * type Movie {
 *   id: ID!
 *   title: String!
 *   yearOfRelease: Int!
 *   director: String!
 * }
 */

const gqlFragment = `



`;
